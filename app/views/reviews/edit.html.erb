<h1>Edit your review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>

  <%= f.submit "Edit a review", class: "btn btn-primary" %>
<% end %>
